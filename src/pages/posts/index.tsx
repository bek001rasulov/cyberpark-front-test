import React from 'react';
import {PostsWrapper} from "./style.ts";

const Posts = () => {
    return (
        <PostsWrapper>
            Posts
        </PostsWrapper>
    );
};

export default Posts;