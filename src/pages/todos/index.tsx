import React from 'react';
import {TodosWrapper} from "./style.ts";

const Todos = () => {
    return (
        <TodosWrapper>
            Todos
        </TodosWrapper>
    );
};

export default Todos;